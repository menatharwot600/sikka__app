# سكة — ابدأ من هنا (النسخة المدموجة الجاهزة)

المشروع ده هو دمج كامل بين:
1. كود سكة نفسه (React + Vite + Supabase)
2. عدّة أدوات Capacitor (الأيقونات، config، سكريبت البناء، ملفات التوقيع)

**كل الدمج ده تم فعليًا جوه الملفات** (مش مجرد وصف) — يعني:

| الملف | الحالة |
|---|---|
| `capacitor.config.ts` | موجود في جذر المشروع |
| `src/lib/pickImage.js` | موجود — كاميرا/معرض حقيقي على أندرويد، ورجوع تلقائي لـ input الملف على الويب |
| `src/lib/deepLinks.js` | موجود — بيلتقط رابط استرجاع كلمة السر لو التطبيق شغّال |
| `src/main.jsx` | بيستدعي `initDeepLinks()` بدري |
| `src/components/AuthScreen.jsx` | رفع صورة بطاقة الدليفري بيستخدم `pickImage()` |
| `src/components/WalletScreen.jsx` | رفع إسكرين إثبات التحويل بيستخدم `pickImage()` |
| `package.json` | باكدجات Capacitor مضافة (core, android, camera, app, splash-screen, status-bar, keyboard) |
| `assets/` | الأيقونة والـ Splash الحقيقيين (مش placeholder) جاهزين لأمر `capacitor-assets generate` |
| `android-manifest-additions.xml`, `build-apk.sh`, `keystore.properties.example`, `build-gradle-signing-snippet.txt`, `download-page.html` | كلهم في جذر المشروع |

## اللي **لازم** يتنفذ على جهازك (مش ممكن من جوه المحادثة)

محتاج: Node.js + Android Studio (SDK) + موبايل أندرويد بكابل USB (أو إيموليتور).

```bash
# 1) تركيب الباكدجات (اللي مضافة بالفعل في package.json)
npm install

# 2) أول مرة بس — إنشاء فولدر android/
npx cap init "سكة" com.seka.app --web-dir=dist
# (لو الأمر ده غيّر capacitor.config.ts، رجّعه للنسخة الموجودة في المشروع)

npm run build
npx cap add android

# 3) توليد الأيقونات بكل الأحجام من فولدر assets/
npx capacitor-assets generate --android

# 4) ضيف الصلاحيات + Deep Link intent-filter من android-manifest-additions.xml
#    يدويًا جوه android/app/src/main/AndroidManifest.xml

# 5) تجربة على موبايل حقيقي متوصل بالكابل
npx cap sync android
npx cap run android
```

تفاصيل كل خطوة (بالذات التوقيع والتوزيع المباشر بدون Google Play) في:
- `دليل-التركيب.md` — الدليل التنفيذي الكامل خطوة بخطوة
- `تجربة-سريعة-على-الموبايل.md` — تجربة debug سريعة من غير Keystore
- `التوزيع-المباشر-بدون-متجر.md` — استراتيجية التوزيع والتحديثات
- `الخطة-والتفاصيل.md` — ملخص الخطة الأصلي (محدّث دلوقتي: الأقسام 1-4
  فيه بقت موجودة فعليًا في الكود، مش بس موصوفة)

## اللي لسه محتاج قرار/إعداد منك (مش كود)

- **دومين حقيقي** تنشر عليه المشروع (Vercel/Netlify) + ملف `assetlinks.json`
  عشان الـ Deep Link بتاع استرجاع كلمة السر يفتح *جوه* التطبيق فعليًا،
  مش في متصفح خارجي. من غيره، رابط الاسترجاع هيفتح المتصفح العادي وهيشتغل
  برضه، بس مش تجربة "نيتيف" كاملة.
- **Keystore** التوقيع — تعمله مرة واحدة بس (`keytool -genkey ...` في
  `دليل-التركيب.md`) واتحفظه في مكان آمن جدًا.
- **Privacy Policy** — رابط فعلي (اختياري بس مهم، التطبيق بيجمع صور بطاقات وبيانات محفظة).
