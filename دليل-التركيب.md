# تركيب Capacitor على سكة — دليل تنفيذي

> **تحديث:** المشروع اللي معاك دلوقتي (النسخة المدموجة) فيه بالفعل كل حاجة
> مذكورة هنا — `capacitor.config.ts`، `src/lib/pickImage.js`،
> `src/lib/deepLinks.js`، فولدر `assets/` بالأيقونات، وباكدجات Capacitor
> مضافة في `package.json`. الخطوات تحت هي اللي **لازم تتنفذ فعليًا على
> جهازك** (Node.js + Android Studio + موبايل بكابل USB) — مش حاجة ممكن
> تحصل من جوه المحادثة.

نفّذها من جوه فولدر المشروع بتاعك على جهازك (محتاج إنترنت + Node.js + Android Studio مُركّبين).

**ملحوظة:** التوزيع هنا مباشر (APK بدون Google Play) — اقرأ
`التوزيع-المباشر-بدون-متجر.md` للتفاصيل الكاملة.

---

## 1) تركيب الباكدجات وإضافة أندرويد

```bash
npm install
npx cap init "سكة" com.seka.app --web-dir=dist
```

> capacitor.config.ts اللي مجهزلك هيتكتب فوقه تلقائي من أمر `cap init` —
> بعد ما يخلص، رجّعه لنفس المحتوى الموجود في المشروع ده (فيه إعدادات
> الـ Splash واللون والـ Keyboard) لو `cap init` غيّره.

```bash
npm run build
npx cap add android
```

---

## 2) الأيقونة والـ Splash Screen

فولدر `assets/` بالأيقونات (icon.png, icon-foreground.png,
icon-background.png, splash.png, splash-dark.png) موجود بالفعل في جذر
المشروع — مفيش داعي تنسخه من حتة تانية.

```bash
npx capacitor-assets generate --android
```

ده هيولّد كل الأحجام (mipmap-mdpi → xxxhdpi) + adaptive icon + splash densities تلقائي.

---

## 3) الصلاحيات و Deep Link

افتح `android/app/src/main/AndroidManifest.xml` وضيف الأجزاء الموجودة في
`android-manifest-additions.xml` (المرفق) — الصلاحيات، والـ intent-filter بتاع رابط استرجاع كلمة السر.

وفي `android/variables.gradle` تقدر تسيب targetSdkVersion زي ما هي أو تحدّثها —
مفيش شرط إجباري 36 هنا لأننا برّه Google Play، بس الأفضل تسيبها حديثة نسبياً
عشان التوافق مع موبايلات جديدة.

---

## 4) مزامنة وتجربة

```bash
npx cap sync android
npx cap open android
```

يفتحلك Android Studio — شغّل على جهاز حقيقي متوصل بالـ USB (مش إيموليتور بس)
وجرّب: تسجيل الدخول، رفع صورة بطاقة الدليفري، رابط استرجاع كلمة السر، تحديث حالة الأوردر لايف.

---

## 5) بناء الـ APK للتوزيع المباشر (بدون Google Play)

1. أنشئ الـ Keystore (**مرة واحدة بس لعمر التطبيق كله** — احفظه بأمان شديد):
   ```bash
   keytool -genkey -v -keystore seka-release.keystore -alias seka -keyalg RSA -keysize 2048 -validity 10000
   ```
2. انسخ `keystore.properties.example` لملف اسمه `android/keystore.properties` واملأه ببياناتك الحقيقية.
   **ضيفه لـ `.gitignore` فوراً — الملف ده فيه باسورد التوقيع.**
3. افتح `android/app/build.gradle` وضيف الأجزاء الموجودة في `build-gradle-signing-snippet.txt`.
4. شغّل سكريبت البناء الجاهز (بيعمل كل حاجة من `npm install` لحد الـ APK النهائي بأمر واحد):
   ```bash
   chmod +x build-apk.sh
   ./build-apk.sh
   ```
5. الملف الناتج:
   ```
   android/app/build/outputs/apk/release/app-release.apk
   ```

---

## 6) النشر (تحميل مباشر)

- ارفع `app-release.apk` على Google Drive (رابط مشاركة عام "Anyone with the link")
  أو استضافتك الخاصة.
- استخدم `download-page.html` (صفحة تحميل جاهزة بهوية سكة) — غيّر رابط الزرار
  لرابط الـ APK الفعلي، وارفعها مع أيقونة `seka-icon-192.png` على نفس الاستضافة.
- شارك رابط الصفحة مع المستخدمين. أول تثبيت هيطلب منهم يفعّلوا
  "السماح بالتثبيت من مصادر غير معروفة" — خطوة واحدة، وشرحها موجود في الصفحة نفسها.

---

## 7) استراتيجية التحديثات

كل تحديث لازم يتوقّع **بنفس الـ Keystore** بالظبط (خطوة 5 تاني)، وترفع
الـ APK الجديد بنفس الرابط. مفيش تحديث تلقائي زي المتجر — التفاصيل والحل
المقترح (بانر "فيه تحديث" جوه التطبيق) موجودين في
`التوزيع-المباشر-بدون-متجر.md`.

---

## ملاحظات مهمة

- **admin.html** مش هيبقى جزء من التطبيق — سيبه يتفتح من المتصفح العادي زي ما هو دلوقتي.
- **sw.js** (Service Worker) مش لازم جوه الـ WebView بتاع Capacitor — تقدر تسيبه، مش هيسبب مشكلة.
- **Privacy Policy**: يفضل يكون عندك رابط فعلي — التطبيق بيجمع بيانات حساسة (صور بطاقات، محفظة، موقع)، حتى برّه Google Play.
- **رابط استرجاع كلمة السر**: عشان يفتح جوه التطبيق فعلاً (Deep Link) لازم يكون عندك دومين حقيقي منشور عليه الأبليكيشن ومظبوط عليه ملف `assetlinks.json` — تفاصيل أكتر في `ابدأ-من-هنا.md`.
- **رفع الصور (بطاقة الدليفري / إثبات التحويل)**: بقت بتستخدم `src/lib/pickImage.js` —
  على أندرويد الحقيقي بتفتح بوب-أب كاميرا/معرض حقيقي، وعلى المتصفح
  بترجع تلقائي لنفس اختيار الملف القديم. تقدر تجرب السلوكين وانت شغّال
  `npm run dev` عادي في المتصفح (هيشتغل بسلوك الويب) وبعد `npx cap run android`
  (هيشتغل بسلوك الكاميرا النيتيف).
