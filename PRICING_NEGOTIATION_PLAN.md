# خطة نظام التفاوض على سعر التوصيل — تطبيق سكة

## 1. الفكرة العامة
حاليًا العميل بيحدد سعر التوصيل وقت إنشاء الأوردر، والدليفري يقدر يقبل أو يرفض بس. المطلوب إضافة نظام تفاوض يسمح للدليفري باقتراح سعر مختلف، والعميل يختار من بين العروض المتاحة.

## 2. تدفق العملية (Flow)

1. العميل يعمل أوردر بسعر مقترح (لازم يكون ≥ الحد الأدنى اللي حدده الأدمن).
2. الأوردر يظهر لكل الدليفري بحالة **"متاح" (pending)**.
3. أي دليفري في شاشة تفاصيل الأوردر (قبل القبول) عنده خياران:
   - **قبول بالسعر الأصلي** → أول دليفري يضغط قبول ياخد الأوردر فورًا (قفل عبر RPC آمن يمنع الـ race condition).
   - **اقتراح سعر تاني (Counter-offer)** → العرض يروح للعميل بس؛ الأوردر يفضل ظاهر لباقي الدليفري في نفس الوقت.
4. الدليفري اللي عرض سعر يقدر **يعدّل عرضه أكتر من مرة** (مش جولة واحدة بس) لحد ما العميل يرد.
5. العميل يشوف **كل العروض** من كل الدليفريين اللي اقترحوا سعر، مع اسم/تقييم كل دليفري، ويقدر يقبل أي عرض يعجبه.
6. أول حدث قبول (من أي طرف: دليفري قابل بالسعر الأصلي، أو عميل قابل عرض) يقفل الأوردر ويلغي باقي العروض/الاحتمالات.

## 3. العمولة
- عمولة التطبيق **ثابتة** (مش نسبة من السعر) — القيمة الافتراضية **2 جنيه** لكل أوردر، بغض النظر عن السعر النهائي المتفق عليه.
- خانة في لوحة الأدمن لتعديل قيمة العمولة وقت ما يحب صاحب التطبيق.

## 4. تعديلات قاعدة البيانات (Supabase)

### جدول `orders`
- عمود `status`: `pending` | `confirmed` | `rejected` | `cancelled`

### جدول جديد `order_offers`
| العمود | النوع | ملاحظات |
|---|---|---|
| `id` | uuid | primary key |
| `order_id` | uuid | ربط بالأوردر |
| `courier_id` | uuid | الدليفري صاحب العرض |
| `offered_price` | numeric | السعر المقترح |
| `status` | text | `pending` \| `accepted` \| `rejected` |
| `created_at` | timestamp | |
| `updated_at` | timestamp | يتحدّث كل مرة الدليفري يعدّل عرضه (upsert على نفس order_id + courier_id) |

### إعدادات الأدمن (`app_settings`)
| العمود | الوصف |
|---|---|
| `min_delivery_price` | الحد الأدنى المسموح به لسعر التوصيل وقت إنشاء الأوردر |
| `commission_amount` | قيمة العمولة الثابتة على كل أوردر (افتراضيًا 2 جنيه) |

### RPC / Database Function آمنة للقبول
- تضمن إن أول قبول بس هو اللي يعدّي (سواء قبول دليفري بالسعر الأصلي أو قبول عميل لعرض)، عن طريق `UPDATE ... WHERE status = 'pending'` وإرجاع خطأ لو الأوردر اتقفل قبل كده.

## 5. الشاشات المتأثرة

### شاشة تفاصيل الأوردر عند الدليفري (قبل القبول)
- زرار **"قبول بالسعر"**
- حقل رقمي + زرار **"اقترح/عدّل سعر"** (يقدر يستخدمه أكتر من مرة)

### شاشة العميل
- قسم جديد **"العروض المقترحة"**: ليستة بكل عرض من كل دليفري (اسم، تقييم، السعر المقترح) مع زرار قبول لكل عرض

### لوحة الأدمن
- حقل رقمي: **الحد الأدنى لسعر التوصيل**
- حقل رقمي: **قيمة العمولة الثابتة**
- زرار حفظ

## 6. خطوات التنفيذ المقترحة

- [x] **1. تعديلات قاعدة البيانات** — `pricing-negotiation-schema.sql`
  - عمود `price` على `orders` + تريجر يفرض الحد الأدنى
  - جدول `app_settings` (الحد الأدنى + العمولة) مع RLS
  - جدول `order_offers` + RLS + Realtime
  - RPCs: `submit_or_update_offer`, `courier_accept_order`, `customer_accept_offer`, `admin_update_app_settings`
  - تريجر خصم العمولة بقى بيقرأ القيمة من `app_settings` بدل الرقم الثابت
  - **لسه محتاج:** تشغيل السكريبت فعليًا على Supabase (SQL Editor)، والتأكد إن سكريبت `docs/supabase-schema.sql` الأساسي كان شغال قبله.

- [x] **2. شاشة الدليفري** — `src/components/CourierScreen.jsx`
  - زرار "قبول بالسعر الأصلي" بقى بينادي `courier_accept_order` (RPC آمن) بدل الـ `update` المباشر
  - كارت كل أوردر بيعرض السعر المقترح من العميل
  - زرار "اقترح سعر تاني" بيفتح حقل رقمي + إرسال → بينادي `submit_or_update_offer`؛ لو عنده عرض قائم بيتحول لـ "عدّل عرضك" ويتعبى بالسعر الحالي
  - بادج "عرضك الحالي: X ج.م" لحد ما العميل يرد
  - اشتراك Realtime على `order_offers` الخاصة بالدليفري نفسه — لو العرض اترفض (العميل قبل حد تاني أو الأوردر اتقفل) بيتشال من الشاشة فورًا مع بانر توضيحي
  - **لسه محتاج:** اختبار فعلي على الداتا الحقيقية بعد تشغيل السكريما، خصوصًا حالة رصيد المحفظة غير الكافي.

- [x] **3. شاشة العميل** — `src/components/CustomerScreen.jsx`
  - حقل "سعر التوصيل المقترح" في فورم إنشاء الأوردر (قيمته الافتراضية = الحد الأدنى من `app_settings`، وبيتبعت مع الأوردر)
  - كل أوردر في تبويب "أوردراتي" بيعرض السعر بتاعه
  - قسم "العروض المقترحة" بيظهر تحت أي أوردر لسه "جديد" وليه عروض معلّقة — اسم كل دليفري وسعره، وزرار "اقبل العرض ده" بينادي `customer_accept_offer`
  - اسم/تليفون الدليفري بيتجابوا عبر RPC جديد `get_my_pending_offers()` (شوف `customer-offers-rpc.sql`) — لأن RLS الأصلية على `profiles` مش بتسمح للعميل يشوف بروفايل دليفري تاني مباشرة
  - اشتراك Realtime على `order_offers` — أي عرض جديد/معدّل/متقبول بيحدّث القايمة لايف من غير refresh
  - **لسه محتاج:** تشغيل `customer-offers-rpc.sql` على Supabase (فوق `pricing-negotiation-schema.sql`)، واختبار فعلي.

- [ ] **4. لوحة الأدمن** — `src/components/AdminScreen.jsx` (لسه ما اتعملتش)
  - حقل رقمي: الحد الأدنى لسعر التوصيل
  - حقل رقمي: قيمة العمولة الثابتة
  - زرار حفظ → بينادي `admin_update_app_settings`

- [ ] **5. اختبار سيناريوهات التزامن** (لسه ما اتعملتش)
  - أكتر من دليفري يضغطوا "قبول بالسعر الأصلي" في نفس اللحظة
  - دليفري يقبل بالسعر الأصلي في نفس وقت ما العميل بيقبل عرض تاني
  - دليفري يعدّل عرضه أكتر من مرة قبل ما العميل يرد
