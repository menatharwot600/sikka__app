# تجربة سريعة على موبايلك (قبل التوقيع والتوزيع)

الخطوة دي **متقدرش تنعمل من عندي** — لازم جهازك فيه Node.js + Android Studio،
وموبايل أندرويد حقيقي متوصل بالكابل. الهدف: تشوف التطبيق شغال زي تطبيق حقيقي
(أيقونة + اسم + splash) قبل ما تتعب نفسك في التوقيع والتوزيع.

**الخبر الكويس:** التجربة دي متحتاجش Keystore ولا توقيع خالص — ده كله بيتأجل
لحد ما تكون عايز تبعت الـ APK لحد تاني.

---

## المتطلبات قبل ما تبدأ

1. **Android Studio** مركّب على جهازك (فيه Android SDK جواه تلقائي)
2. **موبايل أندرويد** عليه:
   - إعدادات → عن الهاتف → دوس على "رقم البناء" 7 مرات (يفعّل "خيارات المطورين")
   - إعدادات → خيارات المطورين → فعّل **USB debugging**
3. **كابل USB** توصل بيه الموبايل بجهازك

---

## الخطوات

من جذر المشروع، بعد ما تكون عملت خطوات 0→4 من `دليل-التركيب.md`
(تركيب Capacitor + الأيقونة + الصلاحيات):

```bash
npm install
npm run build
npx cap sync android
```

بعدين وصّل الموبايل بالكابل، وأول ما يطلعلك على الموبايل نفسه رسالة
"السماح بتصحيح أخطاء USB من هذا الكمبيوتر" دوس **السماح**.

تأكد إن الموبايل متعرف عليه:
```bash
npx cap run android
```

الأمر ده هيسألك تختار الجهاز (لو فيه أكتر من واحد متوصل أو إيموليتور)،
وبعدين هيبني نسخة **debug** (من غير توقيع) ويركّبها ويشغّلها على الموبايل
مباشرة تلقائي.

> لو حصل خطأ إن الموبايل مش ظاهر، جرب:
> ```bash
> adb devices
> ```
> لازم يظهر رقم السريال بتاع موبايلك. لو مش ظاهر، فصل ووصل الكابل تاني
> وتأكد إن USB debugging مفعّل.

---

## اللي المفروض تشوفه

- أيقونة سكة الحقيقية في اللانشر (مش لينك مرفوع)
- Splash screen بلون `#0B1526` ولوجو سكة وقت الفتح
- التطبيق بيفتح full screen من غير شريط متصفح
- جرب: تسجيل دخول، رفع صورة بطاقة الدليفري (المفروض تفتح كاميرا/معرض حقيقي دلوقتي بعد تعديل `pickImage.js`)، تصفح الشاشات

## حاجات متوقعة إنها متشتغلش دلوقتي (لسه)

- **رابط استرجاع كلمة السر** لسه هيفتح المتصفح مش التطبيق — ده محتاج
  الدومين الحقيقي + `assetlinks.json` اللي هنتكلم فيه بعدين
- أي تعديل بتعمله في الكود، لازم تكرر `npm run build && npx cap sync android`
  قبل ما تشغّل `npx cap run android` تاني عشان التغيير يظهر

---

## لو عايز تجرب من غير ما توصل كابل (Live Reload اختياري)

لو عايز تجرب تعديلات بسرعة من غير ما تعمل build كل مرة، تقدر تشغّل:
```bash
npm run dev
```
وتضيف في `capacitor.config.ts` مؤقتاً:
```ts
server: {
  url: 'http://<IP بتاع جهازك على نفس الشبكة>:5173',
  cleartext: true,
}
```
(الموبايل والكمبيوتر لازم يكونوا على نفس شبكة الواي فاي). ده مفيد وقت
التطوير بس — **لازم تشيله قبل بناء نسخة release النهائية**.
