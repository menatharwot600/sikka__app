# التوزيع المباشر (APK بدون Google Play) — إيه اللي بيتغيّر

بما إنك مش هتنزل على Google Play، هنبني **APK موقّع** (مش .aab) وتوزعه إنت بنفسك
عن طريق رابط تحميل مباشر (موقعك، Google Drive، أو أي استضافة ملفات). ده مجاني
100% ومفيش أي رسوم تسجيل.

---

## اللي **بيتلغي** تماماً من الخطة الأصلية

| الخطوة | ليه بتتلغي |
|---|---|
| حساب Google Play Console ($25) | مش محتاجه — إنت مش بتنزل على المتجر |
| Data Safety form | مطلوب بس جوه Play Console |
| targetSdkVersion = 36 إلزامي | ده شرط Google Play بس، مش شرط أندرويد نفسه — تقدر تستهدف أي إصدار، بس الأفضل تسيبه حديث برضه عشان التوافق مع موبايلات جديدة |
| مراجعة Google (ساعات/أيام لكل تحديث) | مفيش مراجعة خالص — أي تحديث بتعمله بيوصل فوراً لما المستخدم يحمّل النسخة الجديدة |
| بناء `.aab` (Android App Bundle) | .aab صيغة مخصوصة لـ Play Store بس — إحنا هنبني `.apk` عادي، ده اللي بيتفتح ويتركب مباشرة على أي موبايل |

## اللي **يفضل زي ما هو**

- كل حاجة في المرحلة 0→5 (Capacitor, الأيقونة, الصلاحيات, الكاميرا, الـ Deep Link) **من غير أي تغيير**.
- الـ Keystore (المفتاح اللي بتوقّع بيه التطبيق) **لازم بالظبط زي الأول** — أي تحديث بتوزعه لازم يكون موقّع بنفس المفتاح، وإلا موبايلات المستخدمين هترفض تحدّثه (هتطلب منهم يمسحوا القديم ويركبوا من جديد).
- Privacy Policy: يفضل بند مهم حتى من غير Play Store — لأنك بتخزن صور بطاقات هوية ومحفظة. مش شرط تقني، بس أفضل عشان الشفافية مع المستخدمين والمراجعة القانونية.

---

## المرحلة 6 (نسخة APK مباشر) — التوقيع والبناء

```bash
keytool -genkey -v -keystore seka-release.keystore -alias seka -keyalg RSA -keysize 2048 -validity 10000
```
احفظ الملف ده وباسوورده في مكان آمن جداً (مش على GitHub، مش في الكود) —
لو ضاع، مش هتقدر توزّع تحديث للناس اللي ركّبت النسخة القديمة؛ هيضطروا يمسحوا التطبيق ويركبوا من الأول.

ظبط `signingConfigs` في `android/app/build.gradle` (نفس الخطوة زي قبل)، وبعدين:

```bash
cd android
./gradlew assembleRelease
```

الملف الناتج هيبقى هنا:
```
android/app/build/outputs/apk/release/app-release.apk
```
ده الملف اللي هتوزعه — مش .aab.

---

## المرحلة 7 (بديلة) — التوزيع المباشر

1. **ارفع الملف** `app-release.apk` على أي حاجة عندها رابط تحميل مباشر:
   - Google Drive (لازم تحط رابط "Anyone with the link can view/download"، مش رابط preview عادي)
   - موقعك بتاع سكة نفسه (لو عندك استضافة) — `https://yourdomain.com/downloads/seka.apk`
   - أي خدمة استضافة ملفات ثانية (Dropbox، إلخ)

2. **صفحة تحميل بسيطة** — الأفضل تعمل صفحة صغيرة فيها زرار "حمّل التطبيق" بدل ما تدي الناس رابط APK عاري، وتشرح فيها خطوة تفعيل "السماح بالتثبيت من مصادر غير معروفة" (تحت).

3. **المستخدم لازم يفعّل "تثبيت من مصادر غير معروفة"** — ده الفرق الحقيقي الوحيد اللي هيحسّه المستخدم:
   - أول ما يفتح ملف الـ APK، أندرويد هيوريله تحذير ويطلب يسمح بالتثبيت من "المصدر" ده (المتصفح أو Drive أو أي حاجة نزّل بيها)
   - خطوة واحدة بس، وبعدها بيتثبت عادي زي أي تطبيق تاني
   - (على عكس تطبيقات Play Store اللي بتتثبت من غير أي تحذير)

---

## استراتيجية التحديثات (بديلة للمرحلة 8)

بما إنك برة Play Store، التحديثات كلها يدوية بالكامل:

1. تعدل الكود → `npm run build` → `npx cap sync android` → `./gradlew assembleRelease`
2. ترفع الـ APK الجديد بنفس الرابط (استبدال الملف القديم)
3. المستخدمين لازم يعرفوا إن فيه تحديث ويحمّلوه بنفسهم يدوي — مفيش إشعار تلقائي "فيه تحديث" زي Play Store، إلا لو تعمل واحد بنفسك (مثلاً: التطبيق يتشيك على رقم إصدار من سيرفرك ويوريله بانر "فيه نسخة جديدة")

> لو التحديثات المتكررة همّتك، فكر تعمل شاشة بسيطة جوه التطبيق تتشيك على أحدث رقم إصدار
> من Supabase (جدول صغير `app_version`) وتوري بانر "فيه تحديث، حمّله من هنا" مع رابط
> — حل بسيط وبيعوّض غياب آلية التحديث التلقائي بتاعة المتجر.
