> **ملحوظة تحديث:** الأقسام 1-9 تحت كانت موصوفة هنا كـ"خلصت" بس الملفات
> الفعلية كانت لسه مش مدموجة جوه مشروع الأبليكيشن. دلوقتي (في النسخة
> اللي معاك) تم الدمج فعليًا — راجع `ابدأ-من-هنا.md` لجدول بكل ملف وحالته
> الحقيقية.

# سكة — خطة تحويل PWA لـ APK حقيقي: ملخص كامل

**الهدف:** تحويل تطبيق سكة من PWA (لينك مرفوع في صورة أبليكيشن) لتطبيق
أندرويد حقيقي — أيقونة حقيقية، اسم حقيقي، ميزات نيتيف (كاميرا، Deep
Link)، وتوزيع مباشر (بدون Google Play، مجاني بالكامل).

**الأداة المستخدمة:** Capacitor (بيغلف نفس كود الـ React الموجود جوه
WebView نيتيف حقيقي، مع Bridge لميزات الموبايل) — بدل TWA اللي كان
هيبقى مجرد "لينك ملفوف" من غير كاميرا حقيقية ولا Deep Links.

---

## الحالة الحالية: إيه اللي خلص فعلاً ✅

### 1) الأيقونة والـ Splash Screen
- اتولّدت من `seka-icon-512.png` الأصلي بتاعك (مش placeholder):
  - `icon.png` (1024×1024) — الأيقونة legacy
  - `icon-foreground.png` + `icon-background.png` — Adaptive Icon (خلفية بلون البراند `#0B1526`)
  - `splash.png` / `splash-dark.png` — شاشة تحميل بنفس هوية سكة

### 2) إعدادات Capacitor
- `capacitor.config.ts` جاهز ومظبوط على:
  - `appId: com.seka.app`, `appName: سكة`
  - StatusBar بلون `#0B1526`
  - SplashScreen بنفس اللون + بدون spinner
  - Keyboard resize للفورمات

### 3) رفع الصور بالكاميرا الحقيقية (بدل input file عادي)
- ملف جديد `src/lib/pickImage.js`: دالة موحّدة —
  - جوه Capacitor → بتفتح بوب-أب "كاميرا / معرض الصور" حقيقي (`@capacitor/camera`)
  - في المتصفح (PWA) → ترجع تلقائي لـ `<input type="file">` القديم، **من غير كسر حاجة**
- اتطبّقت في:
  - `AuthScreen.jsx` — رفع صورة بطاقة الدليفري وقت التسجيل
  - `WalletScreen.jsx` — رفع إسكرين إثبات تحويل الشحن

### 4) Deep Link لرابط استرجاع كلمة السر
- ملف جديد `src/lib/deepLinks.js` + تعديل `main.jsx`:
  - بيلتقط رابط استرجاع الباسورد لو اتفتح من جوه التطبيق (مش المتصفح)
  - بينقله لنفس منطق `App.jsx` الموجود من غير أي تعديل فيه
  - ⚠️ **لسه محتاج دومين حقيقي منشور عليه المشروع + ملف `assetlinks.json`** عشان يشتغل فعليًا (شرح تحت في "المتبقي")

### 5) الصلاحيات المطلوبة (Android Manifest)
- جاهزة كملف مرجعي (`android-manifest-additions.xml`):
  - `CAMERA`, `READ_MEDIA_IMAGES` (لرفع صورة بطاقة الدليفري)
  - intent-filter لرابط استرجاع الباسورد

### 6) استراتيجية التوزيع: مباشر بدون Google Play (بناءً على طلبك)
- اتغيّرت الخطة بالكامل من "نشر على المتجر" لـ "APK مباشر":
  - ملغي: حساب Google Play ($25)، Data Safety form، مراجعة Google، `.aab`
  - بدل منها: بناء `.apk` عادي وتوزيعه بنفسك (رابط تحميل من Drive أو موقعك)
  - المستخدم هيحتاج يفعّل "تثبيت من مصادر غير معروفة" مرة واحدة بس

### 7) أدوات جاهزة للتوقيع والبناء
- `keystore.properties.example` — نموذج بيانات التوقيع
- `build-gradle-signing-snippet.txt` — كود التوقيع لـ `build.gradle`
- `build-apk.sh` — سكريبت واحد بيعمل كل حاجة أوتوماتيك (`npm install` → build → sync → APK نهائي)
- `download-page.html` — صفحة تحميل جاهزة بهوية سكة، فيها شرح لخطوة "مصادر غير معروفة"

### 8) دليل تجربة سريعة (قبل التوقيع والتوزيع)
- طريقة تشغيل نسخة **debug** على موبايل حقيقي متوصل بكابل USB، من غير Keystore خالص:
  ```bash
  npm install && npm run build && npx cap sync android && npx cap run android
  ```

### 9) مشروع مدموج كامل
- كل الكود المعدّل + الإعدادات + الأيقونات اتحطت في مكان واحد
  (`seka-project-complete.zip`) — مفيش داعي تنسخ من أكتر من ملف

---

## المتبقي / الخطوات الجاية 🔜

### أ) تجربة فعلية على موبايل حقيقي (الخطوة الحالية)
لازم تتنفذ على **جهازك إنت** (محتاجة Node.js + Android Studio + موبايل
بكابل USB) — مش ممكن تتنفذ من جوه المحادثة:
```bash
npm install
npm run build
npx cap init "سكة" com.seka.app --web-dir=dist
npx cap add android
npx cap sync android
npx cap run android
```
دليل تفصيلي في `ابدأ-من-هنا.md` و `تجربة-سريعة-على-الموبايل.md`.

### ب) دومين حقيقي + Deep Link كامل
عشان رابط استرجاع كلمة السر يفتح **جوه التطبيق** فعليًا (مش المتصفح):
1. نشر المشروع على دومين حقيقي (Vercel/Netlify — لسه معمول حسب `project-spec.md`)
2. رفع ملف `assetlinks.json` على الدومين ده
3. تعديل `android:host` في intent-filter بالدومين الفعلي

### ج) بناء نسخة release موقّعة (لما تكون جاهز فعليًا)
```bash
keytool -genkey -v -keystore seka-release.keystore -alias seka -keyalg RSA -keysize 2048 -validity 10000
# املأ android/keystore.properties من النموذج
./build-apk.sh
```
الناتج: `android/app/build/outputs/apk/release/app-release.apk`

### د) النشر (تحميل مباشر)
- رفع الـ APK على Google Drive أو استضافة خاصة
- تعديل رابط `download-page.html` ورفعها بجانب الأيقونة

### هـ) Privacy Policy (اختياري بس مهم)
التطبيق بيجمع بيانات حساسة (صور بطاقات هوية، محفظة، موقع) — يفضل رابط
سياسة خصوصية فعلي حتى برّه Google Play، للشفافية مع المستخدمين.

### و) استراتيجية التحديثات
مفيش تحديث تلقائي زي المتجر — كل تحديث محتاج:
1. `npm run build && npx cap sync android`
2. توقيع بنفس الـ Keystore (لازم يفضل نفسه دايمًا)
3. رفع الـ APK الجديد بنفس رابط التحميل
- (اختياري) عمل بانر "فيه تحديث" جوه التطبيق بيتشيك على رقم إصدار من Supabase

---

## ملاحظات مهمة تفضل في بالك

- **admin.html** مش هيبقى جزء من الـ APK — هيفضل يتفتح من المتصفح العادي زي دلوقتي، بدون أي تغيير.
- **الـ Keystore** لازم يتحفظ في مكان آمن جداً — لو ضاع، مش هتقدر توزّع تحديثات للمستخدمين اللي ركّبوا النسخة القديمة (هيضطروا يمسحوا التطبيق ويركبوه من الأول).
- **sw.js** (Service Worker القديم بتاع الـ PWA) مش هيسبب مشكلة جوه WebView بتاع Capacitor — ممكن تسيبه زي ما هو.
